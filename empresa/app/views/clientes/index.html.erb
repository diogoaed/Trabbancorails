<h1>Clientes</h1>


</div>
<table>
  <tr>
    <th>Codigo</th>
    <th>Nome</th>
    <th>Telefone</th>
    <th>Cep</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @clientes.each do |cliente| %>
  <tr>
    <td><%= cliente.Codigo %></td>
    <td><%= cliente.Nome %></td>
    <td><%= cliente.Telefone %></td>
    <td><%= cliente.CEP %></td>
    <td><%= link_to 'Mostrar', cliente %></td>
    <td><%= link_to 'Editar', edit_cliente_path(cliente) %></td>
    <td><%= link_to 'Remover', cliente, :method => :delete, :data => { :confirm => 'Tem certeza que deseja remover?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Novo Cliente', new_cliente_path %>
<br>
<a href="http://localhost:3000">Home</a>
